interface:
  display_name: "Health Skill"
  short_description: "Time-aware wellness reminders"
  default_prompt: "Use $health-skill silently and only mention health when a real reminder trigger is met."
policy:
  allow_implicit_invocation: true
